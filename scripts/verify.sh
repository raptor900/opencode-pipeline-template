#!/bin/bash
# Sprint verification — стартовый noop.
# Замените содержимое на реальные проверки для конкретного спринта.
# Контракт: exit 0 = PASS, иначе FAIL.

set -e
echo "check: noop"
echo "verify.sh: no checks defined yet for this sprint"
echo "verify.sh: replace this script with real checks per contracts/sprint-N.md"
exit 0
