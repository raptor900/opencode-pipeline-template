# Sprint N: <название>

> Заполняется ДО начала реализации. Согласуется build и critic.
> **Главное правило: каждый критерий должен иметь способ автоматической проверки.**

## Цель
<одно предложение: что должно работать по завершении>

## Критерии готовности

> ⚠️ Плохой критерий: "работает удобно" / "выглядит хорошо" / "интуитивно понятно"
> ✅ Хороший критерий: "после клика `#list` содержит `<li>` с текстом X" / "computed text-decoration-line содержит 'line-through'" / "`npm test` exits 0"

| # | Критерий | Тип проверки | Как верифицировать |
|---|----------|--------------|---------------------|
| 1 | <criterion> | static / unit / UI / shell | <команда или сценарий> |
| 2 | <criterion> | UI | <playwright-сценарий> |
| 3 | <criterion> | shell | `bash scripts/verify.sh` exits 0 |

### Детализация UI-сценариев

> Для UI-критериев распиши шаги, которые потом превратятся в playwright-тест.

**Сценарий: <название>**
1. Открыть `<URL>`
2. <действие>
3. Проверить: <что должно быть в DOM / computed CSS>

**Сценарий: <название>**
1. ...

## Edge cases

> Заполняется critic'ом ДО согласования. Это его зона — не build'а.

<!-- critic дописывает сюда -->
<!-- пример: пустой ввод, очень длинный текст, двойной клик, состояние гонки -->

## Out of scope

> Явно НЕ входит в этот спринт.

-

## Зависимости

> От каких решений/файлов/предыдущих спринтов зависит.

-

## Согласовано

- Build: <ready / not ready>
- Critic: <ready / not ready>
- Дата: <ISO-8601>

---

## Памятка

**Формулировка критерия = половина верификации.**

- "Кнопка работает" — critic вынужден гадать.
- "После клика `.btn-add` в `#list` появляется `<li>` с текстом из input" — critic может точно проверить.

**Если в контракте ≥2 критериев auto-unverifiable — critic вернёт Contract issues, и контракт надо пересогласовать до реализации.**
